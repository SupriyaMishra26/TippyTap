import CollectionPage from './CollectionPage';

export default function Restocked() {
  return (
    <CollectionPage
      title="Back in Stock"
      description="Your favorites are back! Shop now before they sell out again"
    />
  );
}
